{
  "name": "model-service",
  "version": "1.0.0",
  "scripts": {
    "start": "bash -c 'source .venv/bin/activate && ENV_FILE=$([ -f .env.local ] && echo .env.local || echo .env) && PORT=$(grep -E \"^PORT\" $ENV_FILE | tr -d \" \" | cut -d= -f2) && uvicorn app.main:app --env-file $ENV_FILE --host ${HOST:-127.0.0.1} --port ${PORT:-8000}'",
    "test": "bash -c 'source .venv/bin/activate && python -m unittest discover -s tests -t .'"
  }
}
