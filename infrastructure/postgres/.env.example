# Copy to /opt/portfolioiq/shared/postgres.env on the server and fill in real
# values. Never commit the filled-in file. Generate the password with:
#   openssl rand -hex 24
POSTGRES_USER=portfolioiq
POSTGRES_PASSWORD=replace-with-a-generated-secret
POSTGRES_DB=portfolio_rebalancing
